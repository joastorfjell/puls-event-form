# Flask
FLASK_SECRET_KEY=PULSMUSII100
PORT=5000

# Email notifications (optional). If not set, app still saves CSV (unless disabled)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_TLS=true
NOTIFY_EMAIL=kontakt@pulsmusikkverksted.no

# Disable CSV writing (useful on serverless like Vercel)
# Set to 1/true/yes/on to skip writing to data/registrations.csv
DISABLE_CSV=

# On Vercel, the platform sets VERCEL=1 automatically; CSV will be disabled even
# if DISABLE_CSV is not set. Configure SMTP vars in Vercel Project Settings if you
# want email notifications from the serverless function
